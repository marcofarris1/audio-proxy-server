{
  "name": "audio-proxy-server",
  "version": "1.0.0",
  "description": "Proxy server for downloading audio files",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "ytdl-core": "^4.11.5",
    "fluent-ffmpeg": "^2.1.2",
    "fs-extra": "^11.2.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}